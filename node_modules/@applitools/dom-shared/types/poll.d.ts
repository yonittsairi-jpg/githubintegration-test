type PollState = {
    value?: any;
    error?: string;
    chunks?: number[];
    from: number;
};
type PollOptions = {
    chunkByteLength?: number;
};
declare function poll(context: Record<string, PollState>, key: string, options?: PollOptions): any;
export = poll;
