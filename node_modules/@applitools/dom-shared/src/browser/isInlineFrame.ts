function isInlineFrame(frame: HTMLIFrameElement): boolean | null {
  return (
    frame.contentDocument &&
    frame.contentDocument.location &&
    !/^https?:$/.test(frame.contentDocument.location.protocol)
  )
}

export = isInlineFrame
