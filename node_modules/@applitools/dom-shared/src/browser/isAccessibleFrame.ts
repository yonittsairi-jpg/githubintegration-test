function isAccessibleFrame(frame: HTMLIFrameElement): boolean {
  try {
    const doc = frame.contentDocument
    return Boolean(doc && doc.defaultView && doc.defaultView.frameElement)
  } catch (err) {
    return false
  }
}

export = isAccessibleFrame
