"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createAuthorizationHeader = void 0;
function createAuthorizationHeader({ referrerUrl, resourceUrl }) {
    if (referrerUrl)
        try {
            const referrer = new URL(referrerUrl);
            const resource = new URL(resourceUrl);
            if (referrer.origin === resource.origin) {
                const { username, password } = referrer;
                if (username && password)
                    return `Basic ${btoa(`${username}:${password}`)}`;
            }
        }
        catch { }
}
exports.createAuthorizationHeader = createAuthorizationHeader;
