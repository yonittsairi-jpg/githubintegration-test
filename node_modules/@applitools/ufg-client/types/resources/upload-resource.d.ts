import type { HashedResource, ContentfulResource } from './resource';
import { type UFGRequests } from '../server/requests';
import { type Logger } from '@applitools/logger';
type Options = {
    requests: UFGRequests;
    concurrency?: number;
    timeout?: number;
    batchingTimeout?: number;
    logger: Logger;
    apiKey?: string;
};
export type UploadResource = (options: {
    resource: ContentfulResource | {
        hash: HashedResource;
        getter: () => Promise<ContentfulResource>;
    };
    logger?: Logger;
}) => Promise<void>;
export declare function makeUploadResource({ requests, batchingTimeout, concurrency, logger: mainLogger, apiKey, }: Options): UploadResource;
export {};
