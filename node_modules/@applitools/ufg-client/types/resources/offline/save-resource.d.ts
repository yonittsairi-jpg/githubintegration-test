import { type Logger } from '@applitools/logger';
import type { UploadResource } from '../upload-resource';
type Options = {
    concurrency?: number;
    timeout?: number;
    batchingTimeout?: number;
    logger: Logger;
    offlineResourcesLocationPath: string;
};
export declare function makeSaveResource({ logger: mainLogger, offlineResourcesLocationPath, batchingTimeout, }: Options): UploadResource;
export {};
