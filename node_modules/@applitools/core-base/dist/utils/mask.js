"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.maskApiKey = void 0;
function maskApiKey(str) {
    if (str.length <= 3) {
        return str; // Do not mask very short strings - it's obviously invalid API key
    }
    const invalidKeys = [
        '',
        'null',
        'undefined',
        'myapikey',
        'yourapikeyhere',
        'yourapikey',
        'applitoolsapikey',
        'nil',
        'empty',
        'apikey',
    ];
    const toAlphanumeric = (s) => s.replace(/[^a-zA-Z0-9]/g, '');
    if (invalidKeys.includes(toAlphanumeric(str).toLowerCase())) {
        return str; // Do not mask obviously invalid API keys
    }
    // For longer strings, take first 2 and last 2 characters
    return str.slice(0, 2) + '****' + str.slice(-2);
}
exports.maskApiKey = maskApiKey;
