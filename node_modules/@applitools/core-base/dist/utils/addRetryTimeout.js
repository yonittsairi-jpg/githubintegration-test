"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addRetryTimeout = void 0;
const DEFAULT_RETRY_TIMEOUT_MINUTES = 60;
/**
 * Helper function to conditionally add retryTimeout to request options.
 * Only adds the property if retryTimeout is defined, preventing undefined values
 * from overwriting base options set during req instance creation.
 */
function addRetryTimeout(retryTimeout) {
    return retryTimeout !== undefined ? { retryTimeout } : { retryTimeout: DEFAULT_RETRY_TIMEOUT_MINUTES * 60 * 1000 };
}
exports.addRetryTimeout = addRetryTimeout;
