import type { CloseSettings } from './types';
import { type Logger } from '@applitools/logger';
import { type EyesRequests, type FunctionalSessionRequests } from './server/requests';
import { type Heartbeat } from './heartbeat';
type Options = {
    requests: EyesRequests | FunctionalSessionRequests;
    done: () => void;
    heartbeat?: Heartbeat;
    logger: Logger;
};
export declare function makeClose({ requests, done, heartbeat, logger: mainLogger }: Options): ({ settings, logger, }?: {
    settings?: CloseSettings | undefined;
    logger?: Logger | undefined;
}) => Promise<void>;
export {};
