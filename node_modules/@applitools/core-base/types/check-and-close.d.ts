import type { Target, CheckSettings, CloseSettings } from './types';
import { type Logger } from '@applitools/logger';
import { type AbortSignal } from 'abort-controller';
import { type EyesRequests } from './server/requests';
import { type Heartbeat } from './heartbeat';
import * as utils from '@applitools/utils';
type Options = {
    requests: EyesRequests;
    done: () => void;
    signal: AbortSignal;
    heartbeat?: Heartbeat;
    logger: Logger;
};
export declare function makeCheckAndClose({ requests, done, signal, heartbeat, logger: mainLogger }: Options): ({ target, settings, logger, }: {
    target: Target;
    settings?: (CheckSettings<utils.Region> & CloseSettings) | undefined;
    logger?: Logger | undefined;
}) => Promise<void>;
export {};
