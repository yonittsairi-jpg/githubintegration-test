import type { HeartbeatSettings } from './types';
import { type Logger } from '@applitools/logger';
import { CoreRequests } from './server/requests';
type Options = {
    requests: CoreRequests;
    logger: Logger;
};
type AcquireSettings = Omit<HeartbeatSettings, 'processId'>;
export type Heartbeat = {
    getOrCreateProcessId: (apiKey: string) => string;
    acquire: (settings: AcquireSettings) => string;
    release: (apiKey: string) => void;
};
export declare function clearAllHeartbeats(): void;
export declare function makeHeartbeat({ requests, logger: mainLogger }: Options): Heartbeat;
export {};
