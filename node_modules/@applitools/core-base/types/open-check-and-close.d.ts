import type { OpenSettings, Target, CheckSettings, CloseSettings } from './types';
import { type Logger } from '@applitools/logger';
import { type CoreRequests } from './server/requests';
type Options = {
    requests: CoreRequests;
    agentId: string;
    logger: Logger;
};
export declare function makeOpenCheckAndClose({ requests, agentId: defaultAgentId, logger: mainLogger }: Options): ({ target, settings, logger, }: {
    target: Target;
    settings: OpenSettings & CheckSettings & CloseSettings;
    logger?: Logger | undefined;
}) => Promise<void>;
export {};
