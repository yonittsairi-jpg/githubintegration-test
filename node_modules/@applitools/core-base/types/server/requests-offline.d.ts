import { Logger } from '@applitools/logger';
import type { CoreRequests } from './requests';
export declare const makeCoreRequestsOfflineWithCache: typeof makeCoreRequestsOffline & {
    getCachedValues(): CoreRequests[];
    setCachedValue(key: any, value: CoreRequests): void;
    clearCache(): void;
    TTL?: number | undefined;
};
declare function makeCoreRequestsOffline({ folderPath, logger: defaultLogger, }: {
    folderPath: string;
    logger?: Logger;
}): CoreRequests;
export {};
