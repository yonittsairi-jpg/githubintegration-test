import type { ImageTarget, ImageSettings } from '../types';
import type { Logger } from '@applitools/logger';
export declare function transformTarget({ target, settings, logger, }: {
    target: ImageTarget;
    settings?: ImageSettings;
    logger?: Logger;
}): Promise<ImageTarget>;
