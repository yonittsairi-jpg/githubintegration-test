import type { AbortSettings } from './types';
import { type Logger } from '@applitools/logger';
import { type AbortController } from 'abort-controller';
import { type EyesRequests, type FunctionalSessionRequests } from './server/requests';
import { type Heartbeat } from './heartbeat';
type Options = {
    requests: EyesRequests | FunctionalSessionRequests;
    done: () => void;
    controller: AbortController;
    heartbeat?: Heartbeat;
    logger: Logger;
};
export declare function makeAbort({ requests, done, controller, heartbeat, logger: mainLogger }: Options): ({ settings, logger, }?: {
    settings?: AbortSettings | undefined;
    logger?: Logger | undefined;
}) => Promise<void>;
export {};
