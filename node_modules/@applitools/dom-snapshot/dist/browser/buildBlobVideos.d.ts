import type { VideoBlob, BlobResult, BuildBlobVideosOptions } from '../types';
declare function buildBlobVideos(imageBlobs: VideoBlob[], { doc, fetchedBlobs, compress, serialize, sessionCache }: BuildBlobVideosOptions): Promise<BlobResult[]>;
export default buildBlobVideos;
