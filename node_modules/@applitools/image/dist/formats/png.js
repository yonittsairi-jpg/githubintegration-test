"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.toPng = exports.fromPngBuffer = exports.extractPngSize = exports.isPngBuffer = void 0;
const buffer_1 = require("buffer");
const png = __importStar(require("png-async"));
const utils = __importStar(require("@applitools/utils"));
function isPngBuffer(buffer) {
    return new TextDecoder().decode(buffer.subarray(12, 16)) === 'IHDR';
}
exports.isPngBuffer = isPngBuffer;
function extractPngSize(buffer) {
    const view = new DataView(buffer.buffer, buffer.byteOffset, buffer.byteLength);
    return { width: view.getUint32(16), height: view.getUint32(20) };
}
exports.extractPngSize = extractPngSize;
/**
 * Dynamically import png-sharp module
 * Tries .js first (production), then .ts (ts-node/dev)
 */
async function importPngSharp() {
    try {
        return await import('./png-sharp.js');
    }
    catch (error) {
        // In ts-node, .js imports fail, try .ts instead
        // Use dynamic string to bypass TypeScript's static import checking
        const tsPath = './png-sharp' + '.ts';
        return await import(tsPath);
    }
}
/**
 * Decode PNG buffer to ImageRaw
 * Tries sharp first (faster), falls back to png-async if sharp fails
 */
async function fromPngBuffer(buffer, defaultLogger) {
    const logger = defaultLogger === null || defaultLogger === void 0 ? void 0 : defaultLogger.extend({ label: 'image/png' });
    try {
        const pngSharp = await importPngSharp();
        const result = await pngSharp.fromPngBufferSharp(buffer);
        logger === null || logger === void 0 ? void 0 : logger.log('Used Sharp for PNG decoding');
        return result;
    }
    catch (error) {
        // Fallback to png-async if sharp fails (SEA, not installed, or runtime error)
        logger === null || logger === void 0 ? void 0 : logger.log('Falling back to png-async for PNG decoding:', error instanceof Error ? error.message : String(error));
        return new Promise((resolve, reject) => {
            const image = new png.Image();
            image.parse(buffer_1.Buffer.from(buffer), (err, image) => {
                if (err)
                    return reject(err);
                resolve(image);
            });
        });
    }
}
exports.fromPngBuffer = fromPngBuffer;
/**
 * Encode ImageRaw to PNG buffer
 * Tries sharp first (22× faster), falls back to png-async if sharp fails
 */
async function toPng(image, defaultLogger) {
    const logger = defaultLogger === null || defaultLogger === void 0 ? void 0 : defaultLogger.extend({ label: 'image/png' });
    try {
        const pngSharp = await importPngSharp();
        const result = await pngSharp.toPngSharp(image);
        logger === null || logger === void 0 ? void 0 : logger.log('Used Sharp for PNG encoding');
        return result;
    }
    catch (error) {
        // Fallback to png-async if sharp fails (SEA, not installed, or runtime error)
        // Uses RLE strategy (default) for optimal performance (~35ms)
        logger === null || logger === void 0 ? void 0 : logger.log('Falling back to png-async for PNG encoding:', error instanceof Error ? error.message : String(error));
        const expectedSize = image.width * image.height * 4;
        const actualSize = image.data.length;
        if (actualSize !== expectedSize) {
            throw new Error(`Image data size mismatch: expected ${expectedSize} bytes for ${image.width}x${image.height}, got ${actualSize} bytes`);
        }
        const wrapper = new png.Image({ width: image.width, height: image.height });
        wrapper.data = buffer_1.Buffer.from(image.data);
        return utils.streams.toBuffer(wrapper.pack());
    }
}
exports.toPng = toPng;
