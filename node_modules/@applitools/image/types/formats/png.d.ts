import type { ImageBuffer, ImageRaw } from '../types';
import type { Size } from '@applitools/utils';
import type { Logger } from '@applitools/logger';
export declare function isPngBuffer(buffer: ImageBuffer): boolean;
export declare function extractPngSize(buffer: ImageBuffer): Size;
/**
 * Decode PNG buffer to ImageRaw
 * Tries sharp first (faster), falls back to png-async if sharp fails
 */
export declare function fromPngBuffer(buffer: ImageBuffer, defaultLogger?: Logger): Promise<ImageRaw>;
/**
 * Encode ImageRaw to PNG buffer
 * Tries sharp first (22× faster), falls back to png-async if sharp fails
 */
export declare function toPng(image: ImageRaw, defaultLogger?: Logger): Promise<ImageBuffer>;
