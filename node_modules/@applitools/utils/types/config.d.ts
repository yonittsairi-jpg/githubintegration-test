export declare function getConfig({ baseDir, paths, params, traverse, strict, logger, }?: {
    baseDir?: string;
    paths?: string[];
    params?: string[];
    traverse?: false | {
        stopDir: string;
    };
    strict?: boolean;
    logger?: any;
}): Record<string, any>;
export declare function populateConfigParams({ config, params }: {
    config: Record<string, any>;
    params?: string[];
}): Record<string, any>;
