import type { Logger } from '@applitools/logger';
import { ECHealthCheckStatus } from './types';
type ConnectivityTestResult = {
    success: boolean;
    timeTaken?: number;
    error?: string;
};
export declare function performEcHealthCheck(options: {
    ecServerUrl: string;
    connectivityTest: () => Promise<ConnectivityTestResult>;
    logger: Logger;
}): Promise<ECHealthCheckStatus>;
export {};
