import { type Hooks } from '@applitools/req';
import { type Logger } from '@applitools/logger';
import type { TunnelClientWorkerSettings } from './types';
export declare function handleLogs({ logger }?: {
    logger?: Logger;
}): Hooks;
export type TunnelLogEventSettings = {
    level: 0;
    eventType: 'TunnelClientSDK';
    event: {
        type: 'Error';
    } & Record<string, any>;
    timestamp?: string;
};
export declare const reportToCoralogix: (input: {
    settings: TunnelLogEventSettings;
    workerSettings: TunnelClientWorkerSettings;
    logger: Logger;
}) => Promise<undefined>;
